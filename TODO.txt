twist is broken

the debug on engine.py for messages sent from submenus looks SUS

This is the config line needed to set smaller blocksize with pw-jack
to be used only once, val=32 in samples
pw-metadata -n settings 0 clock.quantum <VALUE> 


TODO: 

sometimes the pads that are released will not set properly
leading to the the synth being stuck in "set lock" mode
switching pads while going through menus or switching synths generally does weird shit

multi-pad press broken

reduce blocksize etc
warning: 'delay' setting (2 ms) too small for current blocksize (1024 samples); falling back to minimum value (22 ms)

vocoder borken, clouds too

clip selection mode for the seq started, what state is it in???

should look into RT kernel etc for rpi5
yocto project


Trying to debug the janky visual feedback:
Things are def not resetting properly though why I am not entirely sure
It seems that for whatever reason the values for the knobs are being (or reset)
to the wrong ones
This is probably a bug somewhere in the loop that sends the values in seq_metro

updated the update_value_lock func to not use querry when sending vals and it didn't help