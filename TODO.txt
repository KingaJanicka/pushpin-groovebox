This is the config line needed to set smaller blocksize with pw-jack
to be used only once, val=32 in samples
pw-metadata -n settings 0 clock.quantum <VALUE> 


TODO:
UGH now the preset selection stuff is borked 

looks like the clip mode is mostly functional, the UI works
The initial state is recaled as it should but any clips after won't be (getting recalled/saved in weird glitchy ways)

The saving functions for the presets need to be renamed to make sense and tested
After that we can copy the mode and move on to making the slection for sequencer

added label to the menu defs, needs to be documented along with other chagnes like the modmatrix param

